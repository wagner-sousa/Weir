# Caminho para o arquivo .mcp.json no host.
# Quando definido, o Weir montara este arquivo como volume no container
# em vez de usar o ./.mcp.json padrao.
# Util para apontar para configuracoes MCP em outros projetos.
#
# Uso: docker compose up (usa ./.mcp.json como padrao)
#      MCP_CONFIG_SOURCE=~/outro-projeto/.mcp.json docker compose up
#
# MCP_CONFIG_SOURCE=

# Timeout em milissegundos para teste de conexao MCP (padrao: 5000)
MCP_CONNECTION_TIMEOUT=5000

# Timeout em milissegundos para adicao de MCP em CI (padrao: 30000, CI: 5000)
MCP_ADD_TIMEOUT=30000

# Porta do servidor HTTP (padrao: 3000)
# PORT=3000

# Host do servidor HTTP (padrao: 0.0.0.0)
# HOST=0.0.0.0

# Caminho para o arquivo .mcp.json dentro do container.
# Por padrao, o Weir procura por ./.mcp.json no diretorio de trabalho.
# Uso: MCP_CONFIG_PATH=/app/.mcp.json
# MCP_CONFIG_PATH=./.mcp.json

# Ambiente de execucao (development, test, production)
# NODE_ENV=development

# Configuracoes do proxy MCP (modo --proxy)
# WEIR_PROXY_RECONNECT_BASE_DELAY=1000
# WEIR_PROXY_RECONNECT_MAX_DELAY=30000
# WEIR_PROXY_RECONNECT_MAX_RETRIES=10
# WEIR_PROXY_BUFFER_LIMIT=100
# WEIR_PROXY_BACKEND_TIMEOUT=5000
# WEIR_PROXY_KEEPALIVE_MS=15000
# WEIR_PROXY_HTTP_TIMEOUT=30000
# WEIR_PROXY_MAX_BODY_SIZE=1048576
